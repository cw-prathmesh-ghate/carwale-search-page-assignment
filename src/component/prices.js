import React from "react";

export default function(props){
    return(
        <div className="specified-range-container">
        <div className="specified-range">
            {props.keyprice}
        </div>

        </div>
    );
}